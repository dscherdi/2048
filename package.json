{
  "name": "TODO",
  "description": "TODO",
  "version": "0.0.1",
  "scripts": {
    "serve": "sapper export ./deployment && firebase serve",
    "deploy": "sapper export ./deployment && firebase deploy",
    "dev": "sapper dev",
    "build": "sapper build",
    "export": "sapper export",
    "start": "node __sapper__/build",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "run-p --race dev cy:run"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "lodash": "^4.17.15",
    "polka": "next",
    "sirv": "^0.4.0"
  },
  "devDependencies": {
    "cypress": "^4.8.0",
    "file-loader": "^6.0.0",
    "npm-run-all": "^4.1.5",
    "sapper": "^0.27.0",
    "svelte": "^3.0.0",
    "svelte-loader": "^2.9.0",
    "webpack": "^4.7.0"
  }
}
